# Třídy a vlastní mini projekt

**Téma:** Vytváření vlastních tříd, dokončení a prezentace mini projektu  
**Délka:** 90 minut  
**Forma:** Prezenční, počítačová učebna  
**Pomůcky:** Počítače s Visual Studio 2022, projektor, pracovní list, tabule, flash disky

---

## Cíle lekce

Žák:

* chápe, co je **třída** a k čemu slouží v objektově orientovaném programování,
* umí vytvořit vlastní jednoduchou třídu s vlastnostmi a metodami,
* dokáže vytvořit **objekt vlastní třídy** pomocí `new`,
* rozumí rozdílu mezi **třídou** (šablonou) a **objektem** (konkrétní věcí),
* využije všechny dosavadní znalosti k dokončení **vlastního mini projektu**,
* dokáže svůj projekt **prezentovat** před ostatními a vysvětlit, jak funguje.

---

## Obsah lekce

| Čas       | Fáze lekce                                    | Činnost                                                       | Cíl                                          |
| --------- | --------------------------------------------- | ------------------------------------------------------------- | -------------------------------------------- |
| 0–10 min  | **Motivace: Od objektů k vlastním třídám**    | Diskuze o objektech z minulé lekce, přechod k vlastním třídám | Navázat na předchozí znalosti                |
| 10–30 min | **Úvod do tříd – vytvoření vlastní třídy**    | Vysvětlení konceptu třídy, vytvoření třídy `Postava`          | Naučit žáky tvořit vlastní třídy             |
| 30–45 min | **Cvičení: Třída s vlastnostmi a metodami**   | Praktické vytvoření třídy a práce s objekty                   | Procvičit tvorbu tříd a objektů              |
| 45–75 min | **Práce na vlastním mini projektu**           | Samostatná práce na rozšíření nebo vytvoření nového projektu  | Aplikovat všechny naučené koncepty           |
| 75–85 min | **Prezentace projektů**                       | Dobrovolné představení projektů ostatním žákům                | Sdílet výsledky práce a motivovat ostatní    |
| 85–90 min | **Reflexe kurzu a závěr**                     | Shrnutí celého kurzu, rozloučení                              | Uzavřít kurz a motivovat k dalšímu studiu   |

---

## 1. Motivace: Od objektů k vlastním třídám (0–10 min)

### Cíl:

Navázat na předchozí lekci a ukázat, proč potřebujeme vlastní třídy.

### Průběh:

* Připomenutí: Minule jsme pracovali s objekty `Random`, `string`, pole.
* Otázka pro žáky: "Co kdybych chtěl v programu mít hráče, nepřítele nebo auto? Existuje pro to nějaká hotová třída?"
* Vysvětlení: Musíme si vytvořit **vlastní třídu**.

### Na tabuli:

> **Třída** = návod/šablona, jak má objekt vypadat  
> **Objekt** = konkrétní věc vytvořená podle třídy

Příklad:
* Třída `Auto` → objekty: červené auto, modré auto
* Třída `Postava` → objekty: hráč, nepřítel

### Důležité upozornění – změna nastavení projektu:

* Pro práci s třídami **musíme vypnout "top-level statements"**
* Při vytváření nového projektu pro tuto lekci:
  1. Kliknout na **"Create a new project"**
  2. Vybrat šablonu **Console App (C#)**
  3. Pojmenovat projekt
  4. **DŮLEŽITÉ:** Odškrtnout možnost **"Do not use top-level statements"**
  5. Vybrat framework **.NET 8.0 LTS**

* Vysvětlení rozdílu:

**Top-level statements** (lekce 1–4):
```csharp
Console.WriteLine("Hello World!");
```

**Bez top-level statements** (lekce 5):
```csharp
namespace Lekce5
{
    internal class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello, World!");
        }
    }
}
```

* Proč tato změna?
  * Práce s vlastními třídami vyžaduje pochopení struktury programu
  * Všechen kód teď píšeme **dovnitř metody `Main`**
  * Třídy vytváříme **mimo** metodu `Main`

---

## 2. Úvod do tříd – vytvoření vlastní třídy (10–30 min)

**Cíl:** Naučit žáky vytvořit jednoduchou třídu s vlastnostmi a metodami.

### Vytvoření třídy:

1. V projektu kliknout pravým tlačítkem → **Add → Class**
2. Pojmenovat `Postava.cs`
3. Visual Studio vytvoří soubor:

   ```csharp
   class Postava
   {
   
   }
   ```

### Přidání vlastností:

```csharp
class Postava
{
    public string jmeno;
    public int zivoty;
    public int sila;
}
```

### Přidání metody:

```csharp
class Postava
{
    public string jmeno;
    public int zivoty;
    public int sila;

    public void PredstavSe()
    {
        Console.WriteLine("Jsem " + jmeno + " a mám " + zivoty + " životů.");
    }

    public void Utoc(Postava cil)
    {
        Console.WriteLine(jmeno + " útočí na " + cil.jmeno + "!");
        cil.zivoty = cil.zivoty - this.sila;
        Console.WriteLine(cil.jmeno + " má nyní " + cil.zivoty + " životů.");
    }
}
```

### Použití třídy v programu:

V souboru `Program.cs` **uvnitř metody `Main`**:

```csharp
namespace Lekce5
{
    internal class Program
    {
        static void Main(string[] args)
        {
            Postava hrac = new Postava();
            hrac.jmeno = "Rytíř";
            hrac.zivoty = 100;
            hrac.sila = 15;

            Postava nepritel = new Postava();
            nepritel.jmeno = "Skřet";
            nepritel.zivoty = 50;
            nepritel.sila = 10;

            hrac.PredstavSe();
            nepritel.PredstavSe();

            hrac.Utoc(nepritel);
        }
    }
}
```

Výstup:

```
Jsem Rytíř a mám 100 životů.
Jsem Skřet a mám 50 životů.
Rytíř útočí na Skřet!
Skřet má nyní 35 životů.
```

### Diskuze:

* Co se stane, když vytvoříme více objektů stejné třídy?
* Jak se liší jednotlivé objekty?
* K čemu slouží `this`?

---

## 3. Cvičení: Třída s vlastnostmi a metodami (30–45 min)

**Cíl:** Procvičit tvorbu vlastní třídy a práci s objekty.

### Zadání – třída `Zvire`:

Vytvoř třídu `Zvire`, která bude mít:

* Vlastnosti: `jmeno` (string), `vek` (int), `druh` (string)
* Metodu `PredstavSe()`, která vypíše informace o zvířeti
* Metodu `OslavNarozeniny()`, která zvýší věk o 1

### Ukázkový kód:

```csharp
class Zvire
{
    public string jmeno;
    public int vek;
    public string druh;

    public void PredstavSe()
    {
        Console.WriteLine("Jsem " + jmeno + ", je mi " + vek + " let a jsem " + druh + ".");
    }

    public void OslavNarozeniny()
    {
        vek++;
        Console.WriteLine(jmeno + " má narozeniny! Nyní je mu " + vek + " let.");
    }
}
```

Použití v metodě `Main`:

```csharp
static void Main(string[] args)
{
    Zvire pes = new Zvire();
    pes.jmeno = "Rex";
    pes.vek = 3;
    pes.druh = "pes";

    pes.PredstavSe();
    pes.OslavNarozeniny();
}
```

### Rozšíření pro pokročilé:

* Přidat metodu `Zvuk()`, která vypíše zvuk podle druhu
* Vytvořit pole zvířat a procházet ho cyklem
* Přidat konstruktor:

```csharp
public Zvire(string jmeno, int vek, string druh)
{
    this.jmeno = jmeno;
    this.vek = vek;
    this.druh = druh;
}
```

---

## 4. Práce na vlastním mini projektu (45–75 min)

**Cíl:** Aplikovat všechny naučené koncepty v osobním projektu.

### Možnosti projektů:

#### A) Rozšířená hra "Kámen, nůžky, papír"
* Použít třídu `Hrac` s vlastnostmi (jméno, skóre)
* Přidat náhodný výběr pomocí `Random`
* Vést skóre a hrát více kol

#### B) Simulátor souboje postav
* Vytvořit třídu `Postava` s životy, silou
* Implementovat metody pro útok
* Přidat náhodnost do výsledků
* Vytvořit několik postav a nechat je bojovat

#### C) Správce úkolů s prioritami
* Rozšířit správce úkolů z lekce 3
* Vytvořit třídu `Ukol` s vlastnostmi (název, priorita)
* Přidat metody pro označení jako hotové
* Umožnit filtrování podle priority

#### D) Textová adventura
* Vytvořit třídu `Mistnost` s popisem, předměty
* Implementovat průchod mezi místnostmi
* Přidat inventář hráče
* Vytvořit jednoduchou zápletku

#### E) Vlastní nápad
* Žáci mohou navrhnout vlastní projekt
* Měl by využívat: třídy, pole/metody, podmínky, cykly

### Pravidla pro projekt:

Musí obsahovat:
* Alespoň jednu vlastní třídu
* Práci s objekty
* Podmínky (`if`)
* Alespoň jeden cyklus
* Výstup do konzole a vstup od uživatele

Měl by být:
* Funkční (bez kritických chyb)
* Srozumitelný (s komentáři)
* Uložený na flash disk

---

## 5. Prezentace projektů (75–85 min)

**Cíl:** Sdílet výsledky práce a inspirovat ostatní.

### Průběh:

* Dobrovolní žáci (3–5) představí své projekty
* Každý má **2 minuty**:
  * Ukázat běžící program
  * Vysvětlit, co program dělá
  * Ukázat kousek kódu

### Otázky pro prezentující:

* Co bylo nejtěžší?
* Co tě nejvíc bavilo?
* Co bys chtěl ještě přidat?

---

## 6. Reflexe kurzu a závěr (85–90 min)

**Cíl:** Uzavřít celý kurz a motivovat k dalšímu studiu.

### Shrnutí kurzu:

Co jsme se naučili:

1. Základy C# – proměnné, datové typy, výpis textu
2. Podmínky (`if`, `else`) a cykly (`for`, `while`)
3. Pole a metody
4. Náhoda (`Random`) a objekty
5. Vlastní třídy a mini projekt

### Kam dál?

* Webové stránky: learn.microsoft.com, w3schools.com
* YouTube: tutoriály pro začátečníky
* Unity: tvorba her v C#

### Rozloučení:

💬 "Gratulujeme k dokončení kurzu! Ukázali jste, že umíte přemýšlet jako programátoři. Nezapomeňte si uložit své projekty a nebojte se programovat dál."

---

## Diferenciace

| Úroveň žáka | Aktivita                                                                  |
| ----------- | ------------------------------------------------------------------------- |
| Začátečník  | Vytvoří jednoduchou třídu s 2–3 vlastnostmi a jednou metodou             |
| Střední     | Dokončí funkční mini projekt s vlastní třídou a základními funkcemi      |
| Pokročilý   | Vytvoří složitější projekt s více třídami a pokročilými metodami         |

---

## ⚠️ Typické chyby a rady

| Chyba                                     | Co je špatně                          | Správně                                      |
| ----------------------------------------- | ------------------------------------- | -------------------------------------------- |
| `Postava hrac;`                           | Zapomenutí `new`                      | `Postava hrac = new Postava();`              |
| Přístup k nevytvořenému objektu           | Objekt je `null`                      | Vždy nejprve vytvořit pomocí `new`           |
| Zapomenutí `public` u vlastností          | Vlastnosti nejsou přístupné           | Přidat `public` před deklaraci               |
| `class postava`                           | Malé písmeno u názvu třídy            | `class Postava` (třídy velkým písmenem)      |
| Soubor třídy ve špatné složce             | Třída není viditelná                  | Vytvořit přes Add → Class v projektu         |
| Kód mimo metodu `Main`                    | Při vypnutých top-level statements    | Všechen spustitelný kód musí být v `Main`    |
| Zaškrtnuté "Do not use top-level statements" | Projekt vytvořen s top-level statements | Odškrtnout při vytváření projektu         |

---

## 📘 Návaznost

Po tomto kurzu mohou žáci pokračovat:

* **Pokročilé C#:** Práce se soubory, výjimky, LINQ
* **Unity:** Vytváření 2D/3D her
* **Windows Forms / WPF:** Aplikace s grafickým rozhraním
* **ASP.NET:** Webové aplikace