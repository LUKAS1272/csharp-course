# Pole a metody

- **Téma:** Pole (`array`), metody (vlastní funkce)
- **Délka:** 90 minut
- **Forma:** Prezenční, počítačová učebna
- **Pomůcky:** Počítače s Visual Studio 2022, projektor, pracovní list, tabule

---

## 🎯 Cíle lekce

Žák:

* chápe, co je pole a jak se liší od jednotlivé proměnné,
* umí vytvořit pole a pracovat s jeho prvky,
* umí využít cyklus k procházení pole,
* rozumí významu metod – jak rozdělit program na menší části,
* dokáže napsat a zavolat vlastní metodu s parametrem,
* využije pole a metody ve vlastním mini projektu.

---

## 📚 Obsah lekce

| Čas       | Fáze lekce                                           | Činnost                                                   | Cíl                                               |
| --------- | ---------------------------------------------------- | --------------------------------------------------------- | ------------------------------------------------- |
| 0–10 min  | **Motivace: Kolik proměnných potřebuju?**            | Diskuze: jak ukládat více hodnot stejného typu            | Ukázat potřebu polí                               |
| 10–25 min | **Úvod do polí (`array`)**                           | Vysvětlení syntaxe, přístup k prvkům, práce s délkou pole | Naučit žáky používat pole                         |
| 25–40 min | **Cvičení: Oblíbená jídla / čísla**                  | Praktické vytváření a procházení pole pomocí cyklu        | Procvičit práci s poli a cykly                    |
| 40–60 min | **Úvod do metod (vlastní funkce)**                   | Vysvětlení principu metod, parametrů a volání             | Naučit žáky rozdělit program do přehledných částí |
| 60–85 min | **Mini projekt: Výpis a práce s polem pomocí metod** | Kombinace pole a metod v malém projektu                   | Použít oba nové koncepty v praxi                  |
| 85–90 min | **Reflexe a shrnutí**                                | Shrnutí, otázky, diskuze                                  | Upevnit nové poznatky                             |

---

## 1. Motivace: Kolik proměnných potřebuju? (0–10 min)

**Cíl:** Žáci pochopí, že pole je praktické, když potřebujeme více hodnot stejného typu.

* **Na tabuli:**

  ```csharp
  int cislo1 = 5;
  int cislo2 = 10;
  int cislo3 = 15;
  ```

  - "A co kdybych měl 100 čísel?"
  - "Jak bych je vypsal?"

* Vysvětlení: místo stovek proměnných použijeme **pole**, které všechny hodnoty uloží "vedle sebe".

---

## 2. Úvod do polí (10–25 min)

**Cíl:** Seznámit se se syntaxí polí a základní manipulací s jejich prvky.

1. **Základní příklad:**

   ```csharp
   int[] cisla = { 5, 10, 15, 20 };
   Console.WriteLine("První číslo: " + cisla[0]);
   Console.WriteLine("Druhé číslo: " + cisla[1]);
   ```

2. **Vysvětlení:**

   * Pole ukládá více hodnot stejného typu (např. všechna čísla nebo texty).
   * Indexy začínají od **0**.
   * Každý prvek se zapisuje jako `pole[index]`.

3. **Délka pole:**

   ```csharp
   Console.WriteLine("Délka pole: " + cisla.Length);
   ```

4. **Procházení pole pomocí cyklu:**

   ```csharp
   for (int i = 0; i < cisla.Length; i++)
   {
       Console.WriteLine("Prvek číslo " + i + ": " + cisla[i]);
   }
   ```

---

## 3. Cvičení: Oblíbená jídla (25–40 min)

**Cíl:** Procvičit práci s poli a cykly.

* **Zadání:**
  Napiš program, který:

  1. Vytvoří pole o velikosti 3,
  2. Zeptá se uživatele na jeho 3 oblíbená jídla,
  3. Všechna vypíše.

  ```csharp
  string[] jidla = new string[3];

  for (int i = 0; i < jidla.Length; i++)
  {
      Console.WriteLine("Zadej oblíbené jídlo:");
      jidla[i] = Console.ReadLine();
  }

  Console.WriteLine("Tvoje oblíbená jídla:");
  for (int i = 0; i < jidla.Length; i++)
  {
      Console.WriteLine("- " + jidla[i]);
  }
  ```

* **Diskuze:**
  – Co se stane, když použiji jiný index než existuje (např. `jidla[5]`)?
  – Jak zjistit, kolik prvků pole obsahuje?

* **Rozšíření pro pokročilé:**
  Vytvořit pole čísel a spočítat jejich součet pomocí cyklu.

---

## 4. Úvod do metod (40–60 min)

**Cíl:** Naučit žáky vytvářet a volat vlastní metody.

1. **Základní metoda bez parametru:**

   ```csharp
   void Pozdrav()
   {
       Console.WriteLine("Ahoj programátore!");
   }

   Pozdrav(); // zavolání metody
   ```

2. **Metoda s parametrem:**

   ```csharp
   void PozdravJmenem(string jmeno)
   {
       Console.WriteLine("Ahoj, " + jmeno + "!");
   }

   PozdravJmenem("Eliška");
   ```

3. **Metoda pro práci s polem:**

   ```csharp
   void VypisPole(string[] pole)
   {
       for (int i = 0; i < pole.Length; i++)
       {
           Console.WriteLine("- " + pole[i]);
       }
   }

   VypisPole(jidla);
   ```

4. **Diskuze:**

   * K čemu jsou metody užitečné?
   * Jak ulehčují čtení a údržbu kódu?

---

## 5. Mini projekt: Správce úkolů (60–85 min)

**Cíl:** Spojit práci s poli, cykly a metodami v jednom projektu.

### 🧠 Zadání:

Vytvoř program "Správce úkolů", který:

1. Uchovává několik úkolů v poli (např. `string[] ukoly = new string[5];`),
2. Umožní uživateli postupně úkoly zadat,
3. Pomocí metody `ZobrazUkoly()` všechny vypíše,
4. Po vypsání program skončí.

**Ukázkový kód:**

```csharp
string[] ukoly = new string[5];

void ZobrazUkoly(string[] seznam)
{
    Console.WriteLine("Tvůj seznam úkolů:");
    for (int i = 0; i < seznam.Length; i++)
    {
        Console.WriteLine((i + 1) + ". " + seznam[i]);
    }
}

for (int i = 0; i < ukoly.Length; i++)
{
    Console.WriteLine("Zadej úkol č. " + (i + 1) + ":");
    ukoly[i] = Console.ReadLine();
}

ZobrazUkoly(ukoly);
```

**Rozšíření pro pokročilé:**

* Přidat metodu `NajdiUkol(string hledany)`
* Umožnit vypsat jeden úkol podle indexu

---

## 6. Reflexe a shrnutí (85–90 min)

**Otázky pro žáky:**

* Co je to pole a jak se do něj ukládají hodnoty?
* Jaký je rozdíl mezi proměnnou a polem?
* K čemu slouží metoda?
* Jaké výhody má rozdělení programu na menší části?

**Shrnutí:**
Dnes jsme se naučili, jak ukládat více hodnot pomocí **polí** a jak si práci zjednodušit pomocí **metod**.
Pole nám pomáhají pracovat s větším množstvím dat, metody zase dělají program přehlednější a znovupoužitelný.

---

## 🎓 Diferenciace

| Úroveň žáka | Aktivita                                                 |
| ----------- | -------------------------------------------------------- |
| Začátečník  | Vytvoří pole a vypíše jeho prvky pomocí cyklu            |
| Střední     | Použije metodu pro výpis pole                            |
| Pokročilý   | Vytvoří více metod (např. pro vyhledávání nebo počítání) |

---

## ⚠️ Typické chyby a rady

| Chyba                         | Co je špatně                             | Správně / vysvětlení                        |
| ---------------------------   | ---------------------------------------- | ------------------------------------------- |
| `string[] jidla = string[3];` | Chybí `new` při vytváření pole           | `string[] jidla = new string[3];`           |
| `jidla[3] = "Pizza";`         | Index mimo rozsah (3 už neexistuje)      | Poslední prvek je `jidla[2];`               |
| `Console.WriteLine(jidla);`   | Vypíše jen typ pole                      | Nutné použít cyklus pro výpis každého prvku |
| `void metoda;`                | Chybí závorky                            | `void metoda()`                             |
| Zapomenuté volání metody      | Metoda je napsaná, ale nikde se nespustí | Zavolat ji jménem + `()`                    |

---

## 📘 Návaznost

* V další lekci (4️⃣ – *Náhoda a objekty*) žáci objeví, jak generovat **náhodné hodnoty** a jak vytvářet vlastní **objekty**.
  Tyto znalosti využijí k rozšíření svých miniher a programů o realističtější chování.
