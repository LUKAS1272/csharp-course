# Náhoda a objekty (bez vlastních tříd)

* **Téma:** Náhodná čísla (`Random`), jednoduché objekty (už existující třídy a jejich metody)
* **Délka:** 90 minut
* **Forma:** Prezenční, počítačová učebna
* **Pomůcky:** Počítače s Visual Studio 2022, projektor, pracovní list, tabule

---

## 🎯 Cíle lekce

Žák:

* chápe, k čemu se v programech používá **náhoda** (hry, simulace),
* umí použít třídu **`Random`** pro generování náhodných čísel,
* rozumí, že v C# je mnoho věcí **objekt** (např. `Random`, `string`, pole),
* umí vytvořit jednoduchý **objekt** pomocí `new` (např. `new Random()`, `new int[3]`),
* dokáže použít **metody objektů** (`Next()`, `ToUpper()`, `Length`, …),
* využije náhodu a objekty v jednoduchém mini projektu (např. hádání čísla, náhodný výběr jídla).

---

## 📚 Obsah lekce

| Čas       | Fáze lekce                                           | Činnost                                          | Cíl                                    |
| --------- | ---------------------------------------------------- | ------------------------------------------------ | -------------------------------------- |
| 0–10 min  | **Motivace: Náhoda ve hrách a programech**           | Diskuze o hrách, kde se používá náhoda           | Ukázat smysl náhody                    |
| 10–30 min | **Úvod do náhody – třída `Random`**                  | Vysvětlení `Random`, generování náhodných čísel  | Naučit žáky používat náhodu            |
| 30–45 min | **Cvičení: Hody kostkou / náhodný výběr čísla**      | Praktické generování čísel, práce v cyklu        | Procvičit `Random`                     |
| 45–65 min | **Úvod do objektů – věci, co mají metody**           | Co je objekt, ukázka na `Random`, `string`, poli | Pochopit pojem objekt a metody objektů |
| 65–85 min | **Mini projekt: Hádej číslo / náhodný výběr z pole** | Kombinace náhody, objektů a polí                 | Spojit nové koncepty v malém projektu  |
| 85–90 min | **Reflexe a shrnutí**                                | Shrnutí, otázky, diskuze                         | Upevnit nové poznatky                  |

---

## 1. Motivace: Náhoda ve hrách a programech (0–10 min)

**Cíl:** Ukázat, proč chceme v programu náhodu.

Krátká diskuze:

* Kde ve hrách se používá náhoda?

  * hod kostkou,
  * kdy padne bonus,
  * náhodný spawn nepřátel,
  * loot z beden.
* Jak by vypadala hra, kde je všechno **pořád stejné**?

Na tabuli krátké shrnutí:

> Náhoda = prvek překvapení, zábava, simulace „reálného světa“.

Přechod: „V C# máme speciální **objekt**, který umí generovat náhodná čísla – jmenuje se `Random`.“

---

## 2. Úvod do náhody – třída `Random` (10–30 min)

**Cíl:** Umět vytvořit objekt `Random` a generovat s ním náhodná čísla.

1. **Základní příklad:**

   ```csharp
   Random generator = new Random();

   int nahodneCislo = generator.Next(1, 7); // 1 až 6
   Console.WriteLine("Padlo číslo: " + nahodneCislo);
   ```

2. **Vysvětlení:**

   * `Random` je **typ** (třída), která umí náhodu.
   * `new Random()` vytvoří **objekt** – konkrétní „generátor náhody“.
   * `generator.Next(1, 7)` zavolá **metodu objektu**:

     * `1` = dolní mez včetně,
     * `7` = horní mez bez něj → tedy 1–6.

3. **Další ukázky:**

   ```csharp
   int doDeseti = generator.Next(10);      // 0 až 9
   double realne = generator.NextDouble(); // 0.0 až 1.0
   ```

4. **Důležitá rada:**

   * `Random generator = new Random();` – vytvořit **jednou** na začátku programu.
   * Pak tento **jediný objekt** používat všude, kde potřebujeme náhodu.

---

## 3. Cvičení: Hody kostkou / náhodná čísla (30–45 min)

**Cíl:** Procvičit `Random` v jednoduchém programu.

### Základní zadání – hod kostkou 5×

Program:

1. Vytvoří jeden objekt `Random`:

   ```csharp
   Random generator = new Random();
   ```

2. V cyklu 5× „hodí kostkou“ – číslo 1–6.

3. Každý hod vypíše.

Ukázkový kód:

```csharp
Random generator = new Random();

for (int i = 0; i < 5; i++)
{
    int hod = generator.Next(1, 7); // 1–6
    Console.WriteLine("Hod č. " + (i + 1) + ": " + hod);
}
```

### Diskuze:

* Jak změním program, aby házel 10×?
* Jak generovat čísla od 0 do 100?
* Co se stane, když napíšu `generator.Next(1, 6)`?

### Rozšíření pro pokročilé:

* Spočítat, kolikrát padla **šestka**:

  ```csharp
  int pocetSestek = 0;

  for (int i = 0; i < 20; i++)
  {
      int hod = generator.Next(1, 7);
      Console.WriteLine("Hod: " + hod);

      if (hod == 6)
      {
          pocetSestek++;
      }
  }

  Console.WriteLine("Počet šestek: " + pocetSestek);
  ```

---

## 4. Úvod do objektů – věci, co mají metody (45–65 min)

**Cíl:** Uvědomit si, že s mnoha věcmi pracujeme jako s objekty – mají vlastnosti a metody.

1. **Pojmy (jednoduše):**

   * Objekt = **něco v programu**, co:

     * má **data** (např. text, čísla),
     * má **metody** (umí nějakou činnost).
   * Např. `Random`, `string`, pole (`int[]`) – to všechno jsou objekty.

2. **Objekt `Random` – zopakování:**

   ```csharp
   Random generator = new Random();
   int cislo = generator.Next(1, 11); // metoda Next
   ```

   * `generator` je **objekt**.
   * `Next` je **metoda** tohoto objektu – `generator.Next(...)`.

3. **Objekt `string`:**

   ```csharp
   string text = "Ahoj svete";
   Console.WriteLine(text.Length);      // vlastnost Length
   Console.WriteLine(text.ToUpper());   // metoda ToUpper()
   Console.WriteLine(text.ToLower());   // metoda ToLower()
   ```

   Vysvětlení:

   * `text` je objekt typu `string`.
   * `Length` = vlastnost → počet znaků.
   * `ToUpper()` = metoda → vrátí nový text VELKÝMI písmeny.

4. **Pole jako objekt:**

   ```csharp
   int[] cisla = new int[3];
   Console.WriteLine(cisla.Length); // vlastnost objektu pole
   ```

   * `cisla` je objekt.
   * `Length` je vlastnost.

5. **Mini shrnutí na tabuli:**

   ```csharp
   typ    jméno        =  nová hodnota/objekt
   Random generator   =  new Random();
   string text        =  "Ahoj";
   int[]  cisla       =  new int[5];
   ```

   → všechny tyto proměnné jsou **objekty**, se kterými pak pracujeme pomocí `.` (tečka).

---

## 5. Mini projekt: Hádej číslo / náhodný výběr z pole (65–85 min)

**Cíl:** Spojit náhodu, objekty a pole v jednom programu.

### Varianta A – Hádej číslo

1. Program si **vygeneruje náhodné číslo** od 1 do 20.
2. Uživatel hádá číslo, dokud se netrefí.
3. Program odpovídá „Moc malé“, „Moc velké“, „Správně!“.

Ukázkový kód:

```csharp
Random generator = new Random();
int tajneCislo = generator.Next(1, 21); // 1–20

Console.WriteLine("Myslím si číslo od 1 do 20. Zkus ho uhodnout!");

int pokus = 0;

while (pokus != tajneCislo)
{
    Console.Write("Tvůj tip: ");
    string vstup = Console.ReadLine();
    pokus = int.Parse(vstup);

    if (pokus < tajneCislo)
    {
        Console.WriteLine("Moc malé!");
    }
    else if (pokus > tajneCislo)
    {
        Console.WriteLine("Moc velké!");
    }
    else
    {
        Console.WriteLine("Správně! Uhodl jsi číslo.");
    }
}
```

**Použité objekty:**

* `Random generator` – objekt náhody, metoda `Next`.
* `string vstup` – objekt textu, můžeme na něm volat metody (`Length`, …).
* (nepřímo) `Console` – používáme `WriteLine`, `ReadLine`.

---

### Varianta B – Náhodný výběr z pole (např. oblíbená jídla)

1. Vytvoř pole oblíbených jídel / aktivit / filmů.
2. Program několikrát vybere **náhodný prvek** z pole a vypíše ho.

Ukázkový kód:

```csharp
Random generator = new Random();

string[] jidla = { "Pizza", "Sushi", "Řízek", "Špagety", "Salát" };

for (int i = 0; i < 5; i++)
{
    int index = generator.Next(0, jidla.Length); // 0 až Length-1
    string vybraneJidlo = jidla[index];

    Console.WriteLine("Dnes bys mohl jíst: " + vybraneJidlo);
}
```

Diskuze:

* K čemu jsme potřebovali **délku pole** (`jidla.Length`)?
* Co se stane, když napíšu `generator.Next(0, 10)` a `jidla` má jen 5 prvků?

---

## 6. Reflexe a shrnutí (85–90 min)

**Otázky pro žáky:**

* Jak použiji `Random` pro generování náhodného čísla?
* Co je to objekt? Uveď příklad objektu, se kterým jsme dnes pracovali.
* Co znamená `generator.Next(1, 7)`?
* Jak zjistím délku pole?
* U kterých proměnných dnes vidíš metody nebo vlastnosti za tečkou (`.`)?

**Shrnutí:**

* Naučili jsme se používat objekt **`Random`** pro náhodu.
* Viděli jsme, že **proměnné typu `string`, pole nebo `Random` jsou objekty**.
* Objekty mají **vlastnosti** (např. `Length`) a **metody** (např. `Next()`, `ToUpper()`).
* Spojili jsme náhodu, objekty a pole v jednoduché mini hře.

---

## 🎓 Diferenciace

| Úroveň žáka | Aktivita                                                                            |
| ----------- | ----------------------------------------------------------------------------------- |
| Začátečník  | Pouze generuje náhodná čísla (hody kostkou), umí vypsat náhodný index z pole        |
| Střední     | Dokáže napsat program „Hádej číslo“ nebo náhodný výběr jídla z pole                 |
| Pokročilý   | Rozšíří hru: omezený počet pokusů, počítání pokusů, pracuje více s metodami objektů |

---

## ⚠️ Typické chyby a rady

| Chyba / problém                   | Co je špatně / vysvětlení                 | Správně / rada                                                    |
| --------------------------------- | ----------------------------------------- | ----------------------------------------------------------------- |
| Vícekrát `new Random()` za sebou  | Může vracet stejná čísla                  | Vytvořit **jen jeden** objekt `Random` a používat ho všude        |
| `generator.Next(1, 6)` pro kostku | Nikdy nepadne 6                           | Pro kostku použít `generator.Next(1, 7)`                          |
| Index mimo pole                   | `jidla[5]` když pole má 5 prvků (0–4)     | Vždy používat rozsah `0` až `Length - 1`                          |
| Zapomenutá tečka a metoda         | `generator Next(1,7);`                    | Správně: `generator.Next(1, 7);`                                  |
| Zapomenuté závorky u metody       | `generator.Next;`                         | Správně: `generator.Next(1, 7);`                                  |
| Chybný převod textu na číslo      | `int cislo = Console.ReadLine();`         | Použít `int.Parse(Console.ReadLine());` (nebo později `TryParse`) |

---

## 📘 Návaznost

* V další (závěrečné) lekci (5️⃣ – *Pokročilé možnosti objektů a základy tříd*) žáci prohloubí práci s **objekty** a poprvé se setkají s tvorbou vlastních **tříd**.
  Tyto dovednosti využijí k dokončení svého **mini projektu** – rozšíří ho o chytřejší chování, lepší strukturu kódu a udělají z něj „skutečný“ malý program, na který mohou v dalších kurzech navazovat.
