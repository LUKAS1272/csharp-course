### Metodika lekce 2 – **Podmínky a cykly**

**Téma:** Podmínky (`if`, `else`), cykly (`for`, `while`)
**Délka:** 90 minut
**Forma:** Prezenční, počítačová učebna
**Pomůcky:** Počítače s Visual Studio 2022, projektor, pracovní list, tabule

---

### Cíle lekce

Žák:

* chápe, co jsou podmínky v programu a jak je použít (příkaz `if`),
* rozumí použití cyklů pro opakování kódu (příkazy `for` a `while`),
* umí použít podmínky a cykly v jednoduchých programech,
* dokáže napsat program, který reaguje na uživatelský vstup a opakuje akce podle podmínek.

---

### Obsah lekce

| Čas       | Fáze lekce                                      | Činnost                                                                           | Cíl                                                      |
| --------- | ----------------------------------------------- | --------------------------------------------------------------------------------- | -------------------------------------------------------- |
| 0–10 min  | **Motivace: Kvíz – Testování znalostí**         | Aktivita: Blooket nebo Kahoot k opakování základů lekce 1 (proměnné, text, barvy) | Ověřit základní znalosti a připravit žáky na novou látku |
| 10–20 min | **Úvod do čtení uživatelského vstupu**          | Vysvětlení, jak číst vstupy od uživatele pomocí `Console.ReadLine()`              | Seznámit žáky s funkcí pro čtení vstupu                  |
| 20–30 min | **Úvod do podmínek – `if`, `else`**             | Vysvětlení a ukázka: struktura `if`, `else`, podmínky s `==`, `>`, `<`            | Naučit žáky, jak rozhodovat v programech                 |
| 30–50 min | **Cykly – `for` a `while`**                     | Vysvětlení a ukázka cyklů, jak opakovat kód vícekrát                              | Naučit používat cykly pro opakování akcí                 |
| 50–70 min | **Praktický příklad: Hra s podmínkami a cykly** | Vytvoření jednoduché hry, která používá podmínky a cykly (např. hádání čísla)     | Praktické využití podmínek a cyklů                       |
| 70–85 min | **Mini projekt: Kameny, nůžky, papír**          | Vytvoření jednoduché verze hry "Kameny, nůžky, papír" s podmínkami a cykly        | Použití podmínek a cyklů v reálném projektu              |
| 85–90 min | **Reflexe a shrnutí**                           | Sdílení poznatků, opakování klíčových pojmů, dotazy a odpovědi                    | Upevnit naučené koncepty                                 |

---

### 1. Motivace: Kvíz – Testování znalostí (0–10 min)

**Cíl:** Ověřit základní znalosti z předchozí lekce, připravit žáky na novou látku.

* **Příprava:** Lektor předem připraví kvíz na platformě **Blooket** nebo **Kahoot**. Kvíz by měl zahrnovat otázky o základech proměnných, textových výstupech, barvách v konzoli a použití základních funkcí jako `Console.WriteLine` a `Console.Write`.

* **Otázky mohou být např.:**

  1. Co je to proměnná v C#?
  2. Který příkaz použijeme k zobrazení textu na obrazovce?
     a) `Console.WriteLine()`
     b) `Console.DisplayText()`
     c) `Console.Print()`
  3. Co znamená `Console.ForegroundColor = ConsoleColor.Red;`?
     a) Nastaví barvu textu na červenou
     b) Nastaví pozadí na červené
     c) Nic to neudělá

Tento kvíz by měl trvat maximálně 10 minut. Učitel zkontroluje odpovědi a krátce vysvětlí případné nejasnosti.

---

### 2. Úvod do čtení uživatelského vstupu (10–20 min)

**Cíl:** Seznámit žáky s funkcí pro čtení vstupu z konzole a ukázat, jak použít hodnoty zadané uživatelem.

1. **Vysvětlení `Console.ReadLine()`:**

   * `Console.ReadLine()` je metoda, která čte text, který uživatel zadá do konzole.
   * Vstup se vždy uloží jako textový řetězec (datový typ `string`).

2. **Ukázka:**

   * Napište jednoduchý program, který požádá uživatele o zadání jeho jména:

   ```csharp
   Console.WriteLine("Jak se jmenuješ?");
   string jmeno = Console.ReadLine();
   Console.WriteLine("Ahoj, " + jmeno + "!");
   ```

   * Tento kód přečte jméno uživatele a vypíše pozdrav na obrazovku.

3. **Diskuze:**

   * Co se stane, když uživatel zadá něco jiného než text (například číslo)? (Uživatelský vstup je vždy text)
   * Jak bychom mohli tento vstup použít v dalších částech programu? Jak se používá proměnná?

---

### 3. Úvod do podmínek – `if`, `else` (20–30 min)

**Cíl:** Naučit žáky, jak používat podmínky k rozhodování v programech.

1. **Vysvětlení základů podmínky:**

   * **Syntaxe `if` příkazu:**

   ```csharp
   if (podmínka)
   {
       // provede se, pokud je podmínka pravdivá
   }
   ```

   * **Příkaz `else` pro druhou možnost:**

   ```csharp
   if (podmínka)
   {
       // pokud je podmínka pravdivá
   }
   else
   {
       // pokud je podmínka nepravdivá
   }
   ```

2. **Ukázka:**

   * Vytvoření jednoduchého kvízu, který pomocí uživate:

   ```csharp
    string jmeno = "Lukáš";

    Console.WriteLine("Uhodni mé jméno:");
    string vstup = Console.ReadLine(); // Čte vstup jako text

    if (vstup == jmeno)
    {
        Console.WriteLine("Správně!");
    }
    else
    {
        Console.WriteLine("To není mé jméno...");
    }
   ```

3. **Diskuze:** Jaké podmínky mohou být použity? Jak bychom mohli tento příklad vylepšit, aby fungoval i s jinými slovy?

4. **Aktivita:** Studenti rozšíří svůj kvíz o několik dalších otázek.

---

### 4. Cykly – `for` a `while` (30–50 min)

**Cíl:** Naučit používat cykly pro opakování kódu.

1. **Ukázka cyklu `while`:**

   ```csharp
   int i = 0;
   while (i < 5)
   {
       Console.WriteLine("Opakuji se!");
       i++;
   }
   ```

2. **Ukázka cyklu `for`:**

   ```csharp
   for (int i = 0; i < 5; i++)
   {
       Console.WriteLine("Opakuji se!");
   }
   ```

   * Vysvětlení jednotlivých částí cyklu: inicializace, podmínka, inkrementace.

3. **Diskuze:** Kdy použít `for` a kdy `while`? Jaké jsou rozdíly mezi nimi?

---

### 6. Mini projekt: **Kameny, nůžky, papír** (70–85 min)

**Cíl:** Použití podmínek a cyklů v malém projektu.

* **Zadání:**
  Napiš program, který bude simulovat hru "Kameny, nůžky, papír" mezi uživatelem a počítačem.

  * Počítač vybere "kámen", "nůžky" nebo "papír" (program to jednoduše vybere zadaným způsobem).
  * Uživatel zadá svůj výběr.
  * Program rozhodne, kdo vyhrál.

  **Poznámka:** Program nevyužije náhodná čísla, ale bude se zaměřovat na správné použití podmínek. Program bude pracovat s pevně stanovenými hodnotami pro počítačův výběr.

```csharp
string pocitacVyber = "kámen";  // Zde se nevyužívá náhodný výběr, počítač vždy zvolí "kámen"
Console.WriteLine("Vyberte (kámen, nůžky, papír):");
string uzivatelVyber = Console.ReadLine();

if (uzivatelVyber == pocitacVyber)
{
    Console.WriteLine("Remíza! Oba jsme zvolili " + pocitacVyber + ".");
}

if (uzivatelVyber == "kámen")
{
    if (pocitacVyber == "papír")
    {
        Console.WriteLine("Prohráli jste! Počítač zvolil " + pocitacVyber + ".");
    }
    else
    {
        Console.WriteLine("Vyhráli jste!");
    }
}

if (uzivatelVyber == "nůžky")
{
    if (pocitacVyber == "kámen")
    {
        Console.WriteLine("Prohráli jste! Počítač zvolil " + pocitacVyber + ".");
    }
    else
    {
        Console.WriteLine("Vyhráli jste!");
    }
}

if (uzivatelVyber == "papír")
{
    if (pocitacVyber == "nůžky")
    {
        Console.WriteLine("Prohráli jste! Počítač zvolil " + pocitacVyber + ".");
    }
    else
    {
        Console.WriteLine("Vyhráli jste!");
    }
}
```

* **Postupné vylepšování**:
    * Použití `else if` místo `if`
    * Přidání `else`, když uživatel zadá špatný vstup
    * Přidání `while (true)` s počítadlem skóre

---

### 7. Reflexe a shrnutí (85–90 min)

**Cíl:** Upevnit poznatky o podmínkách a cyklech.

* **Otázky pro reflexi:**

  * Co je to podmínka a jak funguje `if`?
  * Jaký je rozdíl mezi cyklem `for` a `while`?
  * Kde jste použili podmínky a cykly v praktických úkolech?

**Shrnutí:**
Dnes jsme se naučili, jak používat podmínky a cykly k tomu, aby naše programy dělaly rozhodnutí a opakovaly akce. To nám umožňuje tvořit interaktivní a dynamické aplikace.

---

### Diferenciace

| Úroveň žáka | Aktivita                                                          |
| ----------- | ----------------------------------------------------------------- |
| Začátečník  | Pouze jednoduché podmínky a cykly, bez složitých herních mechanik |
| Střední     | Použití podmínek a cyklů v jednoduché textové hře                 |
| Pokročilý   | Rozšíření herního designu, přidání více podmínek a cyklů          |

---

### ⚠️ Typické chyby a rady

| Chyba                             | Co je špatně                                  | Správně                               |
| --------------------------------- | --------------------------------------------- | ------------------------------------- |
| Console.ReadLine() nic nedělá     | S hodnotou se nijak nepracuje                 | string vstup = Console.ReadLine();    |
| `if (a = b)`                      | Použití přiřazení místo porovnání             | `if (a == b)`                         |
| `while (true);`                   | Středník za while                             | `while (true)` (while není příkaz)    |
| `for (int i = 0)`                 | Chybí další podmínka a inkrementace  ve `for` | `for (int i = 0; i < 5; i++)`         |
| `for (int i = 0 i < 5 i++)`       | Chybí středníky mezi částmi for               | `for (int i = 0; i < 5; i++)`         |
| `for (int i = 0; i < 5; i++;)`    | Středník navíc za inkrementací                | `for (int i = 0; i < 5; i++)`         |
| Zapomenutý `break` v cyklu        | Program se nikdy nezastaví                    | Použití `break;` pro zastavení cyklu  |

---

## 📘 Návaznost

* V další lekci (3️⃣ – *Seznamy a metody*) žáci použijí seznamy pro ukládnání více hodnot a metody pro zkrácení kódu.